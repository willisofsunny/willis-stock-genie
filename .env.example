# ==========================================
# Willis Stock Genie 環境變數配置
# 複製此文件為 .env 並填入你的實際值
# 注意：.env 文件不要提交到 GitHub
# ==========================================

# ============ LLM API 密鑰 ============
# DeepSeek API 密鑰 (推薦)
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# OpenAI API 密鑰 (備用)
OPENAI_API_KEY=sk-your-openai-key-here

# ============ 應用設置 ============
# 應用環境 (development / staging / production)
ENVIRONMENT=development

# 日誌級別 (DEBUG / INFO / WARNING / ERROR / CRITICAL)
LOG_LEVEL=INFO

# Web 伺服器配置
HOST=0.0.0.0
PORT=8000

# ============ 台股數據源 ============
# FinMind API 設置
FINMIND_API_KEY=your_finmind_key_here

# ============ 資料庫設置 (如需要) ============
# DATABASE_URL=sqlite:///./data/fingenius.db

# ============ 快取設置 ============
# Redis 連接 (如需要)
# REDIS_URL=redis://localhost:6379/0

# ============ 部署平台設置 ============
# Render.com 設置
RENDER_SERVICE_ID=your_render_service_id

# ============ 開發設置 ============
# 調試模式
DEBUG=false

# CORS 允許域名 (用逗號分隔)
# 本地開發
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
# 生產環境（請替換為你的實際 Cloudflare Pages 域名）
# CORS_ORIGINS=https://willis-stock-genie.pages.dev,https://www.yourdomain.com

# ============ 監控和日誌 ============
# Sentry 錯誤追蹤 (可選)
# SENTRY_DSN=https://your-sentry-dsn

# ============ 其他設置 ============
# 請求超時時間 (秒)
REQUEST_TIMEOUT=30

# WebSocket 心跳間隔 (秒)
WS_HEARTBEAT_INTERVAL=30

# 最大 WebSocket 連接數
MAX_WS_CONNECTIONS=100
